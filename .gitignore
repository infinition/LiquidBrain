# Rust build artifacts
/target/

# Compiled binary (generated by `cargo build --release`)
liquidbrain
liquidbrain.exe

# Trained brain files — these can be large and are machine-specific.
# Users should train their own brain locally.
*.brain
readme.main.md
# Cargo lock is committed for reproducible builds (binary crate convention).
# Remove the line below if you want to exclude it.
# Cargo.lock

# Personal notes and knowledge base — not part of the public project.
# Remove this line if you want to track your data/ folder.
/data/

# Editor and OS artifacts
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
Thumbs.db
